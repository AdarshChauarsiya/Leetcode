class Solution {
public:
    int minimizedStringLength(string s) {
        set<int>ans;
        for(int i=0;i<s.size();i++){
            ans.insert(s[i]);
        }
        return ans.size();
    }
};
